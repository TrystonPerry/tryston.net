module.exports = {
  theme: {
    backgroundColor: (theme) => ({
      ...theme("colors"),
      primary: "#ca6dff",
      cryptoalarms: "#f7941d",
      doice: "#46B5D1",
      playspace: "#009DEE",
    }),
    textColor: (theme) => ({
      ...theme("colors"),
      primary: "#ca6dff",
      cryptoalarms: "#f7941d",
      doice: "#46B5D1",
      playspace: "#009DEE",
    }),
  },
  variants: {},
  plugins: [],
};
